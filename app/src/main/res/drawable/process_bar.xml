<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="ring"
    android:useLevel="false">
    <size
        android:width="48dp"
        android:height="48dp" />
    <gradient
        android:startColor="@color/gradient_top"
        android:endColor="@color/gradient_center"
        android:angle="0"
        android:type="sweep" />
</shape>
